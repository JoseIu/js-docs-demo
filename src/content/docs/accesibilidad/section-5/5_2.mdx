---
title: Relación entre DOM y árbol
description: Existen numerosos mitos sobre la accesibilidad digital que llevan a muchas organizaciones a postergar o ignorar su implementación. Desmontemos los más comunes con datos y realidades.
---

import { Aside } from '@astrojs/starlight/components';
import { Code } from '@astrojs/starlight/components';
import domSnippet from './examples/dom-snippet.html?raw';
import arbolTransformacion from './examples/arbol-transformacion.txt?raw';
import ariaEffects from './examples/aria-effects.html?raw';
import hiddenAtributo from './examples/hidden-atributo.html?raw';
import documentoCompleto from './examples/documento-completo.html?raw';
import arbolResultante from './examples/arbol-resultante.txt?raw';
import elementosAnidados from './examples/elementos-anidados.html?raw';
import arbolAnidado from './examples/arbol-anidado.txt?raw';
import cssGeneratedContent from './examples/css-generated-content.html?raw';

## Relación entre DOM y árbol

El árbol de accesibilidad se genera a partir del DOM, pero **no es una copia exacta**. Es una versión filtrada y enriquecida que solo incluye información relevante para las tecnologías asistivas.

### Principio fundamental

**"No todos los nodos del DOM aparecen en el árbol de accesibilidad"**
 El navegador aplica reglas para decidir qué incluir y qué excluir según la semántica, ARIA y estilos CSS.

### Del DOM al árbol: proceso de transformación

#### 📄 DOM (HTML completo)

<Code code={domSnippet} lang="html" title="dom-snippet.html" />

→

#### 🌳 Árbol de Accesibilidad

<Code code={arbolTransformacion} lang="plaintext" title="arbol-transformacion.txt" />

- ❌ div.container → sin rol semántico
- ❌ span.icon → decorativo
- ❌ div oculto → display:none

### Factores que influyen en la exposición

#### 1. Semántica HTML

Los elementos semánticos se incluyen automáticamente

| HTML | Aparece en árbol | Rol expuesto |
| --- | --- | --- |
| `<button>` | ✅ Sí | button |
| `<nav>` | ✅ Sí | navigation |
| `<h1>` | ✅ Sí | heading level 1 |
| `<div>` | ❌ No | — |
| `<span>` | ❌ No | — |

#### 2. Atributos ARIA

ARIA puede añadir, modificar u ocultar nodos

<Code code={ariaEffects} lang="html" title="aria-effects.html" />

#### 3. Estilos CSS

Ciertos estilos excluyen elementos del árbol

| CSS | Aparece en árbol | Razón |
| --- | --- | --- |
| `display: none` | ❌ No | Oculto completamente |
| `visibility: hidden` | ❌ No | No visible |
| `opacity: 0` | ✅ Sí | Transparente pero presente |
| `position: absolute; left: -9999px` | ✅ Sí | Fuera de pantalla pero presente |

#### 4. Atributo `hidden`

El atributo HTML hidden excluye del árbol

<Code code={hiddenAtributo} lang="html" title="hidden-atributo.html" />

### Ejemplo visual completo: transformación paso a paso

#### HTML Original

<Code code={documentoCompleto} lang="html" title="documento-completo.html" />

#### Árbol de accesibilidad resultante

<Code code={arbolResultante} lang="plaintext" title="arbol-resultante.txt" />

Elementos excluidos:

- ❌ `<body>` → No aporta información
- ❌ `<div>` sin rol → Estructura visual únicamente
- ❌ Comentarios HTML → No visibles para el usuario

### Jerarquía y anidamiento

Mantener una estructura HTML lógica asegura que el árbol de accesibilidad sea coherente:

#### DOM con elementos anidados

<Code code={elementosAnidados} lang="html" title="elementos-anidados.html" />

#### Árbol de accesibilidad

<Code code={arbolAnidado} lang="plaintext" title="arbol-anidado.txt" />

### Caso especial: Contenido generado por CSS

<Aside type="caution" title="Importante">
  El contenido generado con `::before` y `::after` NO está en el DOM, pero SÍ puede aparecer en el árbol de accesibilidad:
</Aside>

<Code code={cssGeneratedContent} lang="html" title="css-generated-content.html" />

<Aside type="tip">
  Para excluirlo: `aria-hidden="true"` en el pseudo-elemento no funciona. Usa `content: "";` y añade el asterisco en el HTML.
</Aside>

### Reglas clave de transformación

- ✅ Elementos semánticos HTML siempre se incluyen
- ✅ ARIA puede añadir rol a elementos sin semántica (`<div>`, `<span>`)
- ❌ `aria-hidden="true"` excluye del árbol (y todos sus hijos)
- ❌ `display: none` y `visibility: hidden` excluyen del árbol
- ✅ `opacity: 0` y posicionamiento fuera de pantalla SÍ se incluyen
- ✅ La jerarquía se mantiene, pero se colapsan elementos sin rol
- ⚠️ Los nombres accesibles pueden venir del DOM o ARIA (aria-label, aria-labelledby)

<Aside type="tip" title="Regla práctica">
  Si un elemento no tiene significado semántico (div, span vacíos), no tiene rol ARIA, o está oculto visualmente con CSS específico, no aparecerá en el árbol de accesibilidad.
</Aside>
