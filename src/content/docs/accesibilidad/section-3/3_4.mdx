---
title: Formularios accesibles
description: Existen numerosos mitos sobre la accesibilidad digital que llevan a muchas organizaciones a postergar o ignorar su implementación. Desmontemos los más comunes con datos y realidades.
---
import { Aside } from '@astrojs/starlight/components';
import { Code } from '@astrojs/starlight/components';
import etiquetaCorrecta from './examples/etiqueta-correcta.html?raw';
import etiquetaIncorrecta from './examples/etiqueta-incorrecta.html?raw';
import camposObligatorios from './examples/campos-obligatorios.html?raw';
import mensajesError from './examples/mensajes-error.html?raw';
import fieldsetLegend from './examples/fieldset-legend.html?raw';
import instrucciones from './examples/instrucciones.html?raw';
import formularioAccesible from './examples/formulario-accesible.html?raw';

## Formularios accesibles

Los formularios son elementos críticos de interacción en la web. Un formulario inaccesible puede impedir que usuarios con discapacidades completen tareas esenciales como registrarse, comprar o contactar.

### 1. Etiquetas asociadas a campos

Cada campo de entrada debe tener una etiqueta `<label>` asociada explícitamente mediante el atributo `for`:

#### ✅ Correcto:

<Code code={etiquetaCorrecta} lang="html" title="etiqueta-correcta.html" />

#### ❌ Incorrecto:

<Code code={etiquetaIncorrecta} lang="html" title="etiqueta-incorrecta.html" />

**Beneficio:** Los lectores de pantalla leen la etiqueta cuando el campo recibe el foco. Además, hacer clic en la etiqueta activa el campo.

### 2. Campos obligatorios

Indica visualmente y programáticamente qué campos son requeridos:

<Code code={camposObligatorios} lang="html" title="campos-obligatorios.html" />

### 3. Mensajes de error accesibles

Los errores deben ser claros, específicos y anunciados a los lectores de pantalla:

<Code code={mensajesError} lang="html" title="mensajes-error.html" />

El atributo `aria-describedby` asocia el mensaje de error con el campo, y el lector de pantalla lo anuncia automáticamente.

### 4. Agrupación de campos relacionados

Usa `<fieldset>` y `<legend>` para agrupar campos relacionados, especialmente radios y checkboxes:

<Code code={fieldsetLegend} lang="html" title="fieldset-legend.html" />

### 5. Instrucciones y ayudas

Proporciona instrucciones claras antes de los campos complejos:

<Code code={instrucciones} lang="html" title="instrucciones.html" />

### 6. Atributos ARIA útiles

- `aria-required="true"` — Indica campo obligatorio
- `aria-invalid="true"` — Marca campo con error
- `aria-describedby` — Asocia descripciones o errores
- `aria-labelledby` — Referencia múltiples elementos como etiqueta
- `aria-live="polite"` — Anuncia cambios dinámicos (errores)

### Ejemplo visual de formulario accesible:

Aquí puedes ver y probar un formulario con las mejores prácticas de accesibilidad:

Datos de contacto Nombre completo * Correo electrónico * Te enviaremos una confirmación a este correo Método de contacto preferidoEmailTeléfonoWhatsAppDeseo recibir novedades y ofertasEnviar formulario

### Código del formulario anterior:

<Code code={formularioAccesible} lang="html" title="formulario-accesible.html" />

<Aside type="tip" title="Consejo">
  Prueba el formulario anterior navegando solo con teclado (Tab, Shift+Tab, Enter, Espacio) y con un lector de pantalla para experimentar cómo se anuncia cada elemento.
</Aside>
